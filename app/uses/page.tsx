import { UsesPage } from "@/components/uses/uses-page";

export const metadata = {
  title: "Uses — Bala Sankar Bollineni",
  description: "The tools, software, and setup I use to build products.",
};

export default function Uses() {
  return <UsesPage />;
}
